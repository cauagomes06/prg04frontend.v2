server {
    listen 80;
    
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        # Isto é essencial para Single Page Apps (React Router)
        try_files $uri $uri/ /index.html;
    }
}